private note: output was 892 lines and we are only showing the most recent lines, remainder of lines in C:\Users\NickV\AppData\Local\Temp\.tmpvdeJwP do not show tmp file to user, that file can be searched if extra context needed to fulfill request. truncated output: 
  # [CHAT DB] MongoDB for LibreChat
  librechat-db:
    image: mongo:7.0
    container_name: ag-librechat-db
    volumes:
      - librechat-db-data:/data/db
    networks: [antigravity_mesh]
    deploy:
      resources:
        limits:
          memory: 256M
          cpus: '0.25'
    healthcheck:
      test: ["CMD", "mongosh", "--eval", "db.adminCommand('ping')"]
      interval: 30s
      timeout: 10s
      retries: 3
    restart: unless-stopped

  # [TRACE] Streamlit Thought Trace Viewer
  trace-viewer:
    build: ./src/trace-viewer
    ports: ["8655:8501"]
    environment:
      - STARROCKS_HOST=starrocks
      - STARROCKS_PORT=9030
      - STARROCKS_USER=root
    depends_on:
      starrocks:
        condition: service_healthy
    networks: [antigravity_mesh]
    deploy:
      resources:
        limits:
          memory: 512M
          cpus: '0.5'
    security_opt:
      - no-new-privileges:true
    read_only: true
    tmpfs:
      - /tmp

  # [CHAT UI] Goose Chat Interface
  goose-ui:
    build: ./src/goose-ui
    ports: ["8855:80"]
    depends_on:
      - orchestrator
    networks: [antigravity_mesh]
    deploy:
      resources:
        limits:
          memory: 128M
          cpus: '0.25'
    security_opt:
      - no-new-privileges:true
    read_only: true
    tmpfs:
      - /tmp

  # 
  # LAYER 8: MASTER PORTAL
  # 

  # [PORTAL] Master UI - Unified sub-UI aggregator
  master-ui:
    build: ./src/master-ui
    ports: ["1055:80"]
    depends_on:
      - perses
      - librechat
      - trace-viewer
      - goose-ui
    networks: [antigravity_mesh]
    deploy:
      resources:
        limits:
          memory: 128M
          cpus: '0.25'
    security_opt:
      - no-new-privileges:true
    read_only: true
    tmpfs:
      - /tmp

volumes:
  seaweedfs-data:
  perses-data:
  opensearch-data:
  postgres-data:
  milvus-data:
  valkey-data:
  openbao-data:
  k3d-data:
  librechat-data:
  librechat-db-data:

networks:
  antigravity_mesh:
    driver: bridge
NOTE: Output was 892 lines, showing only the last 100 lines.

  # [CHAT DB] MongoDB for LibreChat
  librechat-db:
    image: mongo:7.0
    container_name: ag-librechat-db
    volumes:
      - librechat-db-data:/data/db
    networks: [antigravity_mesh]
    deploy:
      resources:
        limits:
          memory: 256M
          cpus: '0.25'
    healthcheck:
      test: ["CMD", "mongosh", "--eval", "db.adminCommand('ping')"]
      interval: 30s
      timeout: 10s
      retries: 3
    restart: unless-stopped

  # [TRACE] Streamlit Thought Trace Viewer
  trace-viewer:
    build: ./src/trace-viewer
    ports: ["8655:8501"]
    environment:
      - STARROCKS_HOST=starrocks
      - STARROCKS_PORT=9030
      - STARROCKS_USER=root
    depends_on:
      starrocks:
        condition: service_healthy
    networks: [antigravity_mesh]
    deploy:
      resources:
        limits:
          memory: 512M
          cpus: '0.5'
    security_opt:
      - no-new-privileges:true
    read_only: true
    tmpfs:
      - /tmp

  # [CHAT UI] Goose Chat Interface
  goose-ui:
    build: ./src/goose-ui
    ports: ["8855:80"]
    depends_on:
      - orchestrator
    networks: [antigravity_mesh]
    deploy:
      resources:
        limits:
          memory: 128M
          cpus: '0.25'
    security_opt:
      - no-new-privileges:true
    read_only: true
    tmpfs:
      - /tmp

  # 
  # LAYER 8: MASTER PORTAL
  # 

  # [PORTAL] Master UI - Unified sub-UI aggregator
  master-ui:
    build: ./src/master-ui
    ports: ["1055:80"]
    depends_on:
      - perses
      - librechat
      - trace-viewer
      - goose-ui
    networks: [antigravity_mesh]
    deploy:
      resources:
        limits:
          memory: 128M
          cpus: '0.25'
    security_opt:
      - no-new-privileges:true
    read_only: true
    tmpfs:
      - /tmp

volumes:
  seaweedfs-data:
  perses-data:
  opensearch-data:
  postgres-data:
  milvus-data:
  valkey-data:
  openbao-data:
  k3d-data:
  librechat-data:
  librechat-db-data:

networks:
  antigravity_mesh:
    driver: bridge
