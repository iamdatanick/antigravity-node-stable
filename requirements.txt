# Workflow Orchestration
# hera>=5.16  # Removed: code uses httpx REST, not Hera SDK
# argo-workflows>=6.5  # Removed: code uses httpx REST, not the Python SDK

# Messaging & Data
nats-py>=2.7
boto3>=1.35
asyncpg>=0.29.0  # Replaced psycopg2-binary (LGPL) with asyncpg (Apache-2.0)
pandas>=2.2
aiohttp>=3.9
pymysql>=1.1
dbutils>=3.1
minio>=7.2

# Vector + Cache
pymilvus>=2.4
# valkey>=6.0  # Removed: no Valkey client code exists yet

# Web Framework (A2A HTTP endpoints)
fastapi>=0.109
uvicorn[standard]>=0.27
httpx>=0.27.0,<0.28.0
slowapi>=0.1.9
python-jose[cryptography]>=3.3
pydantic>=2.0

# gRPC (Intel SuperBuilder middleware)
grpcio>=1.62
grpcio-tools>=1.62
grpcio-health-checking>=1.62

# MCP (Model Context Protocol)
mcp>=1.0.0
fastmcp>=2.0.0

# Resilience (self-correction reflex loop)
tenacity>=8.2

# LLM Clients
openai>=1.51.0
anthropic>=0.37.0

# Agentic Workflows (shared hooks, security, orchestration framework)
# Installed from vendored lib/agentic-workflows/ in Dockerfile

# OpenLineage (Marquez integration)
openlineage-python>=1.9

# Testing
pytest>=8.0
pytest-asyncio>=0.23
pytest-cov>=4.1
respx>=0.21

# Observability (OpenTelemetry)
opentelemetry-api>=1.22
opentelemetry-sdk>=1.22
opentelemetry-instrumentation-fastapi>=0.43b0
opentelemetry-instrumentation-grpc>=0.43b0
opentelemetry-instrumentation-aiohttp-client>=0.43b0
opentelemetry-exporter-otlp>=1.22
